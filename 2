#include <stdio.h>
#include<string.h>
int main() {
	/*int ch = 0;
	while (ch = getchar() != EOF) {
		putchar(ch);
	}*///输入ctrl+z就会获取EOF end of file 文件结束标志 实际上是-1
	/*int a = 0;
	* int ch=0; 
	char password[20] = { 0 };
	printf("输入密码\n");
	scanf("%s", password);
	getchar();拿走缓冲区的\n
	printf("确认输入（Y/N）");
	a = getchar();
	if (a == 'Y') {
		printf("确认成功");
	}
	else {
		printf("取消确认");
	}*/
	////while ((ch = getchar()) != '\n') {
	//	;
	////}
	//int ch = 0;
	//while ((ch = getchar()) != EOF) {
	//	if (ch < '0' || ch>'9')
	//		continue;
	//		putchar(ch);
	//}
